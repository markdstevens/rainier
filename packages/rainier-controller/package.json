{
  "name": "@rainier/controller",
  "version": "0.0.1",
  "main": "dist/index.js",
  "description": "The core code for supporting the 'C' - controller - in the SVC framework",
  "author": "Mark Stevens <mdstevens94@gmail.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "clean": "rimraf dist",
    "generateTypes": "tsc --emitDeclarationOnly --declarationDir dist",
    "build": "yarn clean && babel src --out-dir dist --extensions .ts && yarn generateTypes"
  },
  "dependencies": {
    "@rainier/util": "file:../rainier-util",
    "@rainier/view": "file:../rainier-view",
    "@rainier/store": "file:../rainier-store",
    "auto-bind": "^4.0.0"
  },
  "devDependencies": {
    "@babel/plugin-proposal-class-properties": "^7.8.3",
    "@loadable/component": "^5.12.0"
  }
}
